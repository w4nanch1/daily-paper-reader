---
title: AGI Maze as a Benchmark Framework for World-Modeling Agents
title_zh: AGI迷宫：世界建模智能体的基准框架
authors: Alexey Potapov
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00627v1"
tags: ["query:world"]
score: 9.0
evidence: 世界建模代理基准框架
tldr: 大语言模型在部分可观察、有状态的环境中难以维持世界模型。本文提出AGI Maze框架，一种基于格子迷宫的轻量级基准测试，用于评估代理的世界建模能力。初步实验显示，标准LLM在推理时无法内部表示迷宫，即使允许使用消息历史作为工作记忆，代理在简单迷宫上的表现仍远不如人类。该框架为研究代理的世界状态表示提供了标准化环境。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00627v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 433, \"height\": 507, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00627v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 614, \"height\": 735, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00627v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1532, \"height\": 872, \"label\": \"Table\"}]"
motivation: 现有LLM缺乏对外部世界的持久、可操作表征，难以处理部分可观察和需要记忆与结构假设的状态环境。
method: 提出AGI Maze框架，提供系列格子迷宫任务，支持不同难度，并引入允许使用消息历史作为工作记忆的基线代理。
result: 标准LLM无法内部表示迷宫；基线代理使用工作记忆后性能提升，但在小迷宫上仍无法在充裕步数内可靠求解。
conclusion: AGI Maze是有效的世界建模代理基准，揭示当前LLM在状态保持上的不足，需进一步研究。
---

## 摘要
大型语言模型（LLM）是强大的模式补全系统，但其默认操作模式——从静态上下文预测下一个词元——并不能可靠地生成对外部世界的持久、可操作的表征。一旦环境部分可观测、具有状态且需要关于隐藏状态的记忆和结构化假设，许多在文本中看起来像“推理”的任务会变得困难得多。AGI迷宫是一个轻量级框架，用于构建此类环境，无需高维感官输入。它提供了一系列基于网格的迷宫任务，具有简洁的API和多种难度级别。目标是创建基准测试，其中智能体必须学习并使用世界状态表征，而不仅仅是根据现成的观测推断局部规则。我们提供了几个普通LLM在简单迷宫上的初步评估，表明它们在LLM推理时无法在内部表征迷宫。我们还引入了一个基线智能体，允许其利用消息历史作为工作记忆，在智能体运行时构建观测描述。虽然这可以提高性能，但对于LLM智能体来说，即使在人类绰绰有余的步数预算内，仍不足以可靠地解决哪怕是很小的迷宫。

## Abstract
Large language models (LLMs) are powerful pattern-completion systems, but their default operating mode - predicting the next token from a static context - does not reliably produce persistent, manipulable representations of an external world. Many tasks that look like "reasoning" in text become substantially harder once the environment is partially observable, stateful, and requires memory and structured hypotheses about hidden state. AGI Maze is a lightweight framework for building such environments without requiring high-dimensional sensory inputs. It provides a family of grid-based maze tasks with a clean API and multiple difficulty regimes. The goal is to create benchmarks where agents must learn and use world state representations, not just infer a local rule over readily provided observations. We provide an initial evaluation of several vanilla LLMs on simple mazes showing that they fail to represent mazes internally at LLM inference time. We also introduce a baseline agent, which is allowed to use its message history as a working memory to construct descriptions of observations at agentic runtime. Although this can improve performance, it is still insufficient for an LLM agent to reliably solve even small mazes within a step budget that is more than enough for humans.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前大语言模型（LLM）本质上是静态模式补全系统，无法在交互式、部分可观测、有状态的环境中维持并操作外部世界的持久表征。即使看似需要“推理”的语言任务，一旦环境引入隐藏状态和不确定性，LLM 的表现会显著下降。
- **研究背景**：现有基准（如 ARC-AGI-3）多为全观测、单步推理任务，不要求智能体跨时间步维持并更新世界状态。同时，视觉-语言-动作模型虽然能学习短序列动作，但无法解决同步定位与地图构建（SLAM）问题。作者因此提出 AGI Maze 框架，专门测试智能体在世界建模方面的能力：在语言域中构建和利用关于潜在、演变世界状态的表征。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：提供一个轻量级、基于文本的格子迷宫环境，智能体只能通过文本观测（如“被墙挡住”“被河流冲走”）感知环境，必须自己推断位置、墙壁、物品等隐藏状态。
- **关键技术细节**：
  - 网格大小 3×3 到 8×8，包含内部墙、边界“巨石”（只有一个出口）、宝箱和钥匙。
  - 部分可观测性：不显示地图，仅给出起始位置和网格大小。
  - 动态机制增强部分可观测性：
    - **河流**：踏入后强制沿流向移动，方向未知；导致定位模糊。
    - **陷阱循环**：踏入陷阱后传送到循环中下一个陷阱，位置不确定性增大。
    - 限制步数预算（通常为最小所需步数的两倍），迫使智能体高效探索和规划。
  - **扩展机制**：可引入新物品（船、手榴弹、手电筒）、新动作（爆炸、照明）和新格子类型，测试智能体对未知规则的推断能力。
- **基线智能体**：除了普通 LLM（直接基于历史对话预测动作），还设计了**规划智能体**——分两步：先写“笔记”到提示中作为工作记忆（例如当前坐标、探索状态、计划），再调用工具执行动作。

### 3. 实验设计
- **数据集/场景**：使用 AGI Maze 框架中的 **TRAINING** 组迷宫（3×3 到 5×4 大小，含不同机械组合：只有钥匙和宝箱、有河流、有陷阱、混合）。未测试 CLASSIC、EXTENDED、HIDDEN 组。
- **基准方法对比**：
  - **随机行走智能体**：随机选择四个方向。
  - **香草 LLM 智能体**：包括 GPT-4o Mini、GPT-5 Mini、Gemini 3.1 Flash Light、MiniMax M2.7。
  - 另外对更大模型 GPT-5.5 和 Gemini 3.5 Flash 进行了少量评估。
  - **规划智能体**（允许写笔记）：在 GPT-5.5 和 Gemini 3.5 Flash 上测试。
- **评价指标**：在给定步数预算内成功找到出口并拿到宝箱的比例。提供了两个预算阈值（软硬，较宽松的为硬预算的两倍）。

### 4. 资源与算力
- 论文**未明确说明**使用的 GPU 型号、数量或训练时长。仅提到进行了“大量轻量级 LLM 测试”（massive testing of light LLMs），但未提供基础设施细节。因此无法评估训练/推理成本。

### 5. 实验数量与充分性
- **实验数量**：表格 1 报告了 7 个迷宫组（如 S0-01 至 S3-03），每组包含 10 个具体迷宫，每个迷宫每个 LLM 进行 10 次试验，因此每个 LLM 约进行 700 次测试。对更大模型（GPT-5.5、Gemini 3.5 Flash）仅做了少量额外运行。
- **充分性与公平性**：
  - 实验覆盖了不同大小和动态复杂度的简单迷宫，但**未扩展到 CLASSIC 及以上组**，因此只能反映最基础的水平。
  - 使用随机基线进行对比是合理的，但缺乏**人类性能的精确数值对比**（仅定性提到人类在类似步数下可轻松解决）。
  - 规划智能体的设计简单（仅提示写笔记），未探索更复杂的记忆策略或显式地图构建，可能低估了外部记忆的潜力。
  - 总体而言，实验设计初步、规模有限，不足以得出关于更强模型或更复杂谜题的结论，但测试了核心假设。

### 6. 论文的主要结论与发现
- **香草 LLM 在推理时无法内部构建环境状态表征**：在 3×3～5×4 迷宫中，GPT-4o Mini 甚至比随机行走更差；其他模型在小迷宫上略好，但较大迷宫中也不如随机。
- **允许使用消息历史作为工作记忆（写笔记）显著提升强模型性能**：GPT-5.5 从 30% 提升至 60%，Gemini 3.5 Flash 从 40% 提升至 70%。但这对于轻量级 LLM 无帮助（仍为 0%）。
- 即使有了外部记忆，强模型仍然无法可靠解决小迷宫，说明仅靠文本记忆不足以实现鲁棒的世界建模。
- AGI Maze 框架有效暴露出当前 LLM 在**信念跟踪、地图构建、记忆管理、长时规划**等方面的不足。

### 7. 优点
- **轻量纯粹**：避免像素和连续控制，专注于表征和推理瓶颈。
- **部分可观测设计巧妙**：河流、陷阱等动态元素导致强烈的定位歧义，迫使智能体处理不确定性。
- **可扩展性**：支持新物品/机制，可防止过拟合特定规则。
- **API 简洁**：HTTP 接口易于集成，支持人类和智能体在线测试。
- **基线对比合理**：随机行走和普通 LLM 对比清晰，初步验证了核心假设。

### 8. 不足与局限
- **实验覆盖局限**：仅测试了 TRAINING 组（最小最简单的迷宫），未在 CLASSIC、EXTENDED、HIDDEN 组上评估，因此无法验证智能体在高难度或全新机制下的泛化能力。
- **缺乏人类基线精确数据**：虽然提到步数预算根据人类表现设定，但未报告人类成功率，使机器与人类的差距难以量化。
- **规划智能体设计过于简单**：仅提示写坐标和计划，未强制构建全局地图或使用结构化外部表示（如图、约束系统），可能低估了外部世界模型的效果。
- **计算资源未报告**：无法评估实验的可重复性与成本。
- **统计细节不足**：未提供置信区间或统计显著性检验，结果可能存在偶然性。
- **未探索 RL 或专用世界模型智能体**：论文主要聚焦 LLM 局限性，但未测试显式构建世界模型的方法，这恰恰是作者建议的未来方向。

（完）
