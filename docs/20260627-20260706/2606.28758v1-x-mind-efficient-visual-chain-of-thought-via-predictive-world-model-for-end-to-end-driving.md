---
title: "X-Mind: Efficient Visual Chain-of-Thought via Predictive World Model for End-to-End Driving"
title_zh: "X-Mind: 通过预测世界模型实现高效视觉思维链的端到端驾驶"
authors: "Bohao Zhao, Chengrui Wei, Guangfeng Jiang, Ruixin Liu, Xuejie Lv, Liu Liang, Sutao Deng, Xiuyang Fan, Pengkun Zheng, Jinyun Zhou, Rui Guo, Hanpeng Liu, Yutong Zheng, Yi Guo, Xinlong Zheng, Qingyu Luo, Zhuangzhuang Ding, Yu Zhang, Hang Zhang, Xianming Liu"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28758v1"
tags: ["query:world"]
score: 9.0
evidence: 端到端驾驶中的预测世界模型
tldr: 现有VLA模型缺乏预测未来状态的能力，依赖被动感知-动作映射。本文提出X-Mind框架，将预测世界模型内化为视觉链式思维，强制模型在动作前先想象未来演化。通过BEV草图与驾驶先验融合的紧凑表示，结合深度压缩自编码器将12帧未来预测压缩至96token；并采用递归块扩散方案将去噪步骤折叠进骨干网络一次前向传播。在大规模真实数据上验证，达到竞争性端到端驾驶性能，实现低延迟、资源高效的大模型认知推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28758v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1445, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 593, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1158, \"height\": 651, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1407, \"height\": 580, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 882, \"height\": 969, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28758v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1452, \"height\": 940, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28758v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1215, \"height\": 217, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28758v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1309, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28758v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1214, \"height\": 218, \"label\": \"Table\"}]"
motivation: 当前VLA模型缺乏预测能力，现有世界模型方法存在级联延迟或未能深度嵌入前向推理。
method: 将预测世界模型内化为视觉链式思维，用BEV草图与驾驶先验作为紧凑心智画布，配合深度压缩自编码器和递归块扩散加速生成。
result: 在大规模真实世界数据上验证，X-Mind达到竞争性端到端驾驶性能，且延迟低、适合资源受限平台。
conclusion: X-Mind成功将大规模认知推理部署到资源受限的车辆平台，实现高效实用的端到端驾驶。
---

## 摘要
预测未来状态对于自主智能体至关重要，但当前的视觉-语言-动作（VLA）模型从根本上缺乏这种能力，它们依赖于反应式感知-动作映射。虽然集成预测世界模型（PWM）可以弥补这一不足，但现有方法要么引入难以承受的级联延迟，要么作为浅层终端任务，未能深入嵌入前向推理。为了使VLA模型具备这种推理能力，我们提出了X-Mind。该框架不是将PWM视为外部辅助模块，而是将其内化为视觉思维链（Visual CoT）。通过在动作之前强制执行世界展开，模型被约束为先想象未来的演化，从而产生一个稳健基于环境动态且意识到其动作未来后果的驾驶策略。这里的挑战在于效率，我们从两个方面解决。首先，我们引入了一种紧凑的视觉思维表示：一种抽象草图，将鸟瞰图（BEV）布局与抽象驾驶先验（如导航意图和交通规则）融合。模型不是展开密集的未来帧，而是将这种草图作为心理画布进行推理；借助深度压缩自编码器（DC-AE），12帧的未来展开被减少到仅96个token，缓解了长上下文计算瓶颈。其次，为了进一步加速生成，我们提出了一种循环块扩散方案，该方案将去噪步骤跨大型驾驶模型的层展开，将迭代细化折叠到骨干网络的一次前向传播中。在大规模真实世界数据上训练和验证，X-Mind实现了具有竞争力的端到端驾驶性能，使其成为一种非常实用、低延迟的解决方案，成功地将大规模认知推理直接部署到资源受限的车辆平台上。

## Abstract
Predicting future states is essential for autonomous agents, yet current Vision-Language-Action (VLA) models fundamentally lack this capability, relying instead on reactive perception-action mapping. While integrating Predictive World Models (PWMs) addresses this gap, existing approaches either incur prohibitive cascaded latency or act as shallow terminal tasks that fail to deeply embed forward-looking reasoning. To endow VLA models with this reasoning capability, we propose X-Mind. Rather than treating PWMs as an external auxiliary module, this framework internalizes them as the Visual Chain-of-Thought (Visual CoT). By enforcing a world rollout prior to action, the model is constrained to imagine future evolution first, yielding a driving policy that is robustly grounded in environmental dynamics and aware of the future consequences its actions will unfold. The challenge here is efficiency, and we tackle it on two fronts. First, we introduce a compact representation of visual thinking: an abstract sketch that fuses a Bird's-Eye-View (BEV) layout with abstract driving priors (e.g., navigation intents and traffic rules). Rather than rolling out dense future frames, the model reasons over this sketch as a mental canvas; aided by a Deep Compression Autoencoder (DC-AE), a 12-frame future rollout is reduced to merely 96 tokens, alleviating the long-context computational bottleneck. Second, to accelerate generation further, we propose a recurrent block diffusion scheme that unrolls the denoising steps across the layers of the large drive model, folding iterative refinement into the backbone's one forward pass. Trained and validated on large-scale real-world data, X-Mind achieves competitive end-to-end driving performance, which makes it a highly practical, low-latency solution that successfully deploys large-scale cognitive reasoning directly onto resource-constrained vehicle platforms.

---

## 论文详细总结（自动生成）

# X-Mind: 通过预测世界模型实现高效视觉思维链的端到端驾驶——论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前的视觉-语言-动作（VLA）模型在端到端自动驾驶中主要依赖被动感知-动作映射，缺乏显式预测未来状态的能力。这种反应式范式无法真正理解环境动态的因果演化，导致模型容易产生捷径学习（shortcut learning），在复杂场景中表现脆弱。
- **现有方法不足**：虽然整合预测世界模型（PWM）被广泛认为是补齐这一差距的共识，但现有做法存在两种缺陷：(1) 级联式PWM（先预测再规划）会引入难以承受的推理延迟；(2) 将PWM作为浅层终端任务附加在VLA模型末端，其监督信号难以有效传播到深层语言模型骨干，核心层仍依赖于稀疏的真值轨迹，无法从根本上赋予模型前向推理能力。
- **本文目标**：提出X-Mind框架，将预测世界模型**内化为视觉链式思维（Visual CoT）**，强制模型在执行动作之前先想象未来的环境演化，从而产生基于环境动力学且意识到自身动作后果的驾驶策略。同时要在保持低延迟的前提下实现这一认知推理，使其能够部署到资源受限的车辆平台。

## 2. 论文提出的方法论

### 2.1 核心思想
- 将PWM内嵌于大型驾驶模型（Large Drive Model）中，作为显式的视觉思维链（Visual CoT）：在生成动作之前，模型必须先生成一个未来的抽象草图，作为“心智画布”进行推理。
- 通过两个关键设计解决效率瓶颈：(a) 紧凑的视觉思维表示——抽象草图，融合BEV布局与驾驶先验；(b) 循环块扩散（RBD）机制——将去噪步骤跨LLM层展开，实现单次前向传播的未来生成。

### 2.2 关键技术细节

#### (a) 视觉思维表示（Visual Thinking Representation）
- **抽象草图**：不是预测密集的未来图像或3D高斯泼溅，而是预测一个结构化的鸟瞰图（BEV），其中包含两类信息：
  - **物理场景元素**：自车、周围动态车辆、道路拓扑（车道线、边界）。
  - **抽象驾驶先验**：动态交通灯状态（左上角信号面板）、自适应导航意图（青色导航通道/路径）、速度合规曲线（左下角速度条：绿色为实际速度，白色为限速余量，红色为超速程度）。
- **压缩**：通过深度压缩自编码器（DC-AE）将12帧未来抽象草图压缩为仅96个token，解决长上下文计算瓶颈。

#### (b) 循环块扩散（Recurrent Block Diffusion, RBD）
- **动机**：传统扩散模型需要多次迭代前向传播，不适合实时应用。RBD利用LLM的层次结构（浅层捕捉低级模式、深层编码高级语义）与扩散去噪过程（从粗到细）的天然同构性。
- **训练阶段——层流匹配（Layer Flow Matching, LFM）**：
  - 定义非均匀时间步调度：{t₀,...,t₅} = {0, 0.1, 0.2, 0.4, 0.7, 1.0}。
  - 在指定注入层，将草图token的隐藏状态替换为噪声与真值潜在特征zt = (1-t)·ε + t·z₁的线性插值（z₁为DC-AE编码的真值草图）。
  - 从各提取层的输出中预测速度场vk，通过轻量级Transformer编码器（参数共享）映射回潜在空间。
- **推理阶段——渐进去噪**：
  - 从纯噪声z₀~N(0,I)开始，通过欧拉积分在各层深度逐步更新：z_{k+1}=z_k+(t_{k+1}-t_k)·v_k。
  - 最终得到完全去噪的潜在表示z₅，由冻结的DC-AE解码器重建未来草图序列。

#### (c) 联合优化目标
- 总损失：L_total = λ_WM L_WM + λ_plan L_plan
- L_WM包含：
  - 层流匹配损失：L_flow = (1/K) Σ||v_k - v*||²（用于速度场监督）。
  - 重构损失：L_img（随机选择一个去噪层解码，计算MSE+LPIPS）。
- L_plan：对运动学控制动作（纵向加速度、横摆角速度）施加L1损失，确保非完整约束下的可执行性。

## 3. 实验设计

### 3.1 数据集与场景
- **数据集**：内部大规模真实世界自动驾驶数据集，包含约28万小时连续驾驶记录，分割为3400万个视频片段。
- **传感器设置**：7个摄像头（前鱼眼、前窄、左前、右前、左后、右后、后），实现360°覆盖。
- **场景分布**：约86.8%城市道路，13.2%高速公路。
- **数据规模**：视觉输入被token化为13.8T tokens（采用X-Foresight处理协议）。所有实验在1/8子集上进行。

### 3.2 对比方法
- **标准VLA基线（Base）**：直接预测轨迹，无未来推理。
- **Base + Image**：额外预测未来图像特征。
- **Base + 3DGS**：额外预测3D高斯泼溅。
- **Base + Sketch (Single Step)**：将草图作为目标，但仅在最后一层进行单步去噪。
- **Base + Sketch (RBD)**：本文提出的递归块扩散方案。

### 3.3 评估指标
- **ADE（平均位移误差）**：分横向（Lateral）和纵向（Longitudinal）在6秒预测时间。
- **FID**：用于评估未来预测的视觉质量。
- **推理延迟**：相对基线的倍数。

## 4. 资源与算力

- 论文中**未明确说明使用的GPU型号、数量、训练时长**。仅提到“训练在1/8数据集子集上进行”，未提供具体算力消耗细节。这在一定程度上限制了可复现性评估。

## 5. 实验数量与充分性

### 5.1 主要实验组
1. **不同场景表示对比**（表1）：Base、Base+Image、Base+3DGS、Base+Sketch（Ours）——共4组。
2. **扩散架构对比**（表2）：Base、Base+Sketch (Single Step)、Base+Sketch (RBD)——共3组。
3. **时间预测目标消融**（表3）：当前帧、未来1帧、未来12帧——共3组。

### 5.2 定性分析
- 图4：比较RBD与单步扩散的未来预测质量。
- 图5：结构化草图的GT数据示例（交通灯、导航路径、速度合规）。
- 图6：真实场景下的轨迹可视化对比（安全、导航、交通灯、障碍物规避）。

### 5.3 充分性与公平性评价
- **充分性**：实验覆盖了方法论的关键设计点（表示形式、扩散策略、预测时间长度），消融设计合理，定量分析到位。
- **客观性**：所有对比使用了相同的骨干网络和训练配置，公平性较好。但存在以下局限：
  - **缺乏公开基准**：仅使用内部数据集，未在nuScenes、Waymo等公开数据集上验证，可能引入领域偏差。
  - **缺乏与最新端到端方法（如UniAD、VAD等）的直接外部比较**，仅对比自身设计的基线。
  - **指标覆盖不足**：仅使用ADE和FID，未报告碰撞率、舒适性、执行成功率等关键驾驶安全性指标。

## 6. 论文的主要结论与发现

### 6.1 抽象草图的优势
- 相比图像特征（3584额外token）和3DGS（3072额外token），抽象草图仅需96个额外token，却取得最佳横向/纵向ADE（0.1765/1.1849 vs 基线0.2399/1.2979），推理延迟仅为基线的1.1倍。
- 证明了紧凑、结构化的表示能够有效过滤视觉噪声，保留规划所需的空间语义。

### 6.2 RBD的有效性
- RBD vs 单步扩散：FID从67.30降至9.59（大幅提升预测质量），同时ADE进一步降低（横向0.1765 vs 0.1783），推理延迟几乎相同（1.1倍基线）。
- 定性结果（图4）显示RBD能生成时空一致、甚至推理出GT中缺失的动态物体的运动轨迹，表明模型学到了物理动力学和因果推理，而非简单记忆。

### 6.3 预测未来而非重建当下是关键
- 预测12帧未来比重建当前帧或仅预测1帧在未来，ADE显著更优（横向0.1765 vs 0.1866/0.1840），尽管FID略高。这证明世界模型的核心优势来自**预测性生成**，而非视觉逼真度。

## 7. 优点

- **方法创新性强**：将预测世界模型内化为视觉思维链，属于认知推理方向的新范式，区别于传统级联或终端附加方式。
- **高效性设计突出**：通过抽象草图+DC-AE实现极致的token压缩（12帧→96 token），结合RBD将扩散步骤折叠进LLM单次前向传播，同时保持低延迟（推理延迟仅增加0.1倍），适合车载部署。
- **实验系统全面**：针对三个关键设计维度（表示形式、扩散架构、预测时序）分别进行了消融，并提供了丰富定性结果（图4-6），支持主要结论。
- **可解释性好**：抽象草图直观融合BEV布局与驾驶先验，开发者可诊断障碍物占用、车道连接等，易于理解模型行为。
- **未来方向明确**：论文指出了联合采样控制与草图、自监督学习等扩展方向，为后续研究提供清晰路线。

## 8. 不足与局限

1. **依赖真值标注**：抽象草图的生成需要高质量的GT（包括交通灯、导航路径、速度合规等），限制了模型扩展到更多未标注场景。论文在“未来工作”中承认这点，计划引入自监督学习。
2. **未在公开基准测试**：仅在内部数据集上验证，缺乏与主流方法（如UniAD、DriveVLM等）在nuScenes、Waymo Open Dataset上的公平比较。内部数据分布可能存在偏差，结论的泛化性尚未证明。
3. **算力开销不透明**：未报告GPU型号、数量、训练时间，无法评估训练可复现性和资源需求。
4. **评估指标不完整**：仅使用ADE和FID，缺少碰撞率、舒适性、决策合理性、时间一致性等更全面的驾驶质量指标。
5. **RBD的时间步调度固定**：论文手动定义了非均匀时间步，未探索自适应或可学习的调度策略，可能不是最优。
6. **对比基线不足**：只与自身构造的原始基线和简单变体对比，未与最新的专用世界模型+规划方法（如DriveDreamer, DriveWM等）直接比较，缺乏领域内最具竞争力方法的横向对比。
7. **现实部署考虑**：虽然声称低延迟，但未提供实际车载推理的时间（如毫秒级），也未讨论对计算资源的具体要求（如内存、功率）。

（完）
