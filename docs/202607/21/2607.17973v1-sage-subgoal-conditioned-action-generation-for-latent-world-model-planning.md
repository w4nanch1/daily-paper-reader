---
title: "SAGE: Subgoal-Conditioned Action Generation for Latent World Model Planning"
title_zh: "SAGE: 子目标条件动作生成用于潜世界模型规划"
authors: "Letian Cheng, Qi Zhang, Yisen Wang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17973v1"
tags: ["query:world"]
score: 9.0
evidence: 为潜在世界模型规划生成子目标条件动作
tldr: "潜在世界模型在长时程规划中因随机候选动作探索效率低而性能下降。本文提出SAGE，用子目标条件生成代替随机初始化，通过预测不同时长子目标来引导动作序列生成，再经冻结世界模型评估。在PushT和OGBench Cube任务上，长时程成功率分别从12.7%提升至64.7%、从26.7%提升至67.3%，显著改善规划质量。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17973v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1385, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17973v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1427, \"height\": 539, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17973v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1411, \"height\": 494, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17973v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1379, \"height\": 172, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17973v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1320, \"height\": 325, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17973v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 854, \"height\": 603, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17973v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1234, \"height\": 173, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17973v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1018, \"height\": 317, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17973v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1239, \"height\": 575, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17973v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 918, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17973v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 254, \"label\": \"Table\"}]"
motivation: 长时程规划中固定候选预算面对指数级动作空间，随机初始化难以生成高质量候选序列。
method: 引入先验条件规划器，用目标条件生成器预测不同时长子目标，并基于子目标生成候选动作序列，由冻结世界模型评估。
result: "在PushT目标偏移150时成功率从12.7%升至64.7%，OGBench Cube从26.7%升至67.3%。"
conclusion: 子目标条件动作生成有效提升潜在世界模型的长时程规划性能，同时保持短时程优势。
---

## 摘要
潜世界模型通过学习条件动作的预测动力学，并将其用作内部模拟器来想象和评估候选动作序列，已成为一种强大的规划范式。然而，随着规划视野的增长，性能越来越受到提案质量的限制：固定的候选预算必须在指数级增长的动作空间中进行搜索，使得世界模型难以接触到高质量的候选未来用于评估。本文提出了一种先验条件规划器，用结构化引导替代随机提案初始化。在每个规划阶段，目标条件生成器预测指定时长内下一个可达到的潜子目标，然后用于条件生成候选动作序列。为了捕获跨时间尺度的语义信息，我们使用不同时长的子目标作为先验，平衡细粒度的局部控制与更高层的长视野进展。然后，冻结的世界模型在执行前评估并优化这些子目标条件的提案。在PushT和OGBench Cube上的实验表明，将潜子目标分解与先验条件动作生成相结合，显著改善了长视野规划，同时保持了强大的短视野性能。具体而言，当目标偏移量为150时，PushT的成功率从12.7%提升到64.7%，OGBench Cube的成功率从26.7%提升到67.3%。

## Abstract
Latent world models have emerged as a powerful planning paradigm by learning action-conditioned predictive dynamics and using them as internal simulators to imagine and evaluate candidate action sequences. However, as the planning horizon grows, performance becomes increasingly constrained by proposal quality: a fixed candidate budget must search an exponentially larger action space, making it difficult to expose the world model to high-quality candidate futures for evaluation. In this paper, we introduce a prior-conditioned planner that replaces random proposal initialization with structured guidance. At each planning stage, a goal-conditioned generator predicts the next reachable latent subgoal for a specified duration, which is then used to condition the generation of candidate action sequences. To capture semantic information across temporal scales, we use subgoals of varying durations as priors, balancing fine-grained local control with higher-level long-horizon progress. Then the frozen world model evaluates and refines these subgoal-conditioned proposals before execution. Experiments on PushT and OGBench Cube show that coupling latent subgoal decomposition with prior-conditioned action generation substantially improves long-horizon planning while preserving strong short-horizon performance. To be specific, when the target offset is $150$, it raises PushT success from $12.7\%$ to $64.7\%$ and OGBench Cube success from $26.7\%$ to $67.3\%$.

---

## 论文详细总结（自动生成）

# SAGE: Subgoal-Conditioned Action Generation for Latent World Model Planning 论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：潜世界模型（latent world model）在长时程规划（long-horizon planning）中性能严重下降。根本原因在于规划时候选动作序列（proposals）是从随机分布初始化的，随着规划步数增加，动作空间指数级增长，固定预算下难以采样到高质量的动作序列，导致世界模型无法接触到有希望的未来状态进行有效评估。
- **背景**：现有方法如LeWM、DINO-WM等通过学习潜动态模型进行规划，但均受限于提案质量。PRISM虽引入学习到的动作先验，但缺乏对任务结构的分解。FF-JEPA预测子目标但仅将其作为优化目标而非动作生成条件。
- **整体含义**：本文提出**SAGE**，通过将长时程任务分解为多个不同时间尺度的可达到子目标，并基于这些子目标生成动作候选，从而显著提升长时程规划性能，同时不修改或重训练世界模型。

## 2. 方法论：核心思想与技术细节

### 2.1 核心思想
- 将规划分为两步耦合决策：
  1. **预测可达到的潜子目标**（subgoal）：针对当前规划时长τ，预测未来τ步后应该达到的潜状态。
  2. **子目标条件动作生成**：基于预测的子目标，生成条件化的动作候选序列。
- 冻结的世界模型（LeWM）负责对这些子目标条件下的提案进行评估和CEM优化。

### 2.2 关键组件

#### (1) 子目标生成器（Subgoal Generator）
- 输入：当前历史潜表征 \(z_{t-k:t}\)、低维状态 \(x_t\)、远目标潜表征 \(z_{g_{t+\delta}}\)、剩余偏移量δ和请求时长τ。
- 输出：预测的局部子目标潜表征：  
  \[
  \hat{z}_{t+\tau} = z_{g_{t+\delta}} + D_\psi(z_{t-k:t}, x_t, z_{g_{t+\delta}}, \delta, \tau)
  \]
- 使用4层Transformer解码器，残差形式使输出保持在LeWM潜空间内。
- 训练损失：SmoothL1 + 余弦相似度，匹配冻结LeWM编码的真实局部未来潜表征。

#### (2) 子目标条件动作生成器（Subgoal-Conditioned Action Generator）
- 输入：历史、状态、预测子目标 \(\hat{z}_{t+\tau}\)、远目标、δ和τ。
- 输出：高斯混合模型（GMM），描述整个τ步动作选项：  
  \[
  q_\phi(a_{t:t+\tau-1} | \cdots) = \sum_{m=1}^M \pi_m \mathcal{N}(a_{t:t+\tau-1}; \mu_m, \Sigma_m)
  \]
- 使用3层Transformer解码器，时长掩码控制激活的动作查询数量（每5个环境动作对应一个查询）。
- 训练损失：专家动作序列的对数似然（负对数似然）。

#### (3) 在线规划流程
- 总规划步数H拆分为时长序列 \((\tau_1,\dots,\tau_R)\)。
- 每阶段：子目标生成器预测当前时长下目标，动作生成器采样K个候选动作选项，LeWM评估并CEM优化（30轮），执行选中的τ步动作，然后进入下一阶段。
- 支持多个时长（如15、20、25）共享同一生成器，可混合调度。

## 3. 实验设计

### 3.1 数据集与场景
- **PushT**：平面接触操作任务（推动T形物体到目标位姿）。训练14,948个episodes，测试1,869个episodes，动作维度2。
- **OGBench Cube**：机器人操作任务（移动方块并放置）。训练8,000个episodes，测试1,000个episodes，动作维度5。

### 3.2 Benchmark设置
- 测试规划步数H ∈ {25, 50, 75, 100, 125, 150}（如果训练数据支持）。
- 每个H构造3个固定评估集（种子32,42,52），每个包含50个起始-目标对。
- 环境预算：PushT为2H步，Cube为H步。
- 所有方法共享相同的冻结LeWM检查点、300个候选、30个精英、30轮CEM。

### 3.3 对比方法
- **Base CEM**：LeWM标准高斯CEM提案。
- **PRISM**：状态条件动作先验 + CEM。
- **Generator-only**：使用生成的局部子目标，但动作生成仍用随机高斯初始化（消融）。
- **SAGE prior top**：直接执行动作生成器最高权重选项，不经过LeWM选优和CEM（消融）。
- **Full SAGE**：完整方法（子目标生成 + 条件动作生成 + LeWM CEM优化）。

## 4. 资源与算力

论文中**未明确说明**使用的GPU型号、数量或训练时长。仅在附录中提及使用BF16精度和AdamW优化器，批量大小为128，训练400k步（每个组件）。未提供总计算量估计。

## 5. 实验数量与充分性

- **主要实验**：在PushT和Cube两个任务上，对6个规划步长（H=25到150）报告了成功率，每个H有3个种子，总计3×6=18个条件，每个条件50个查询，结果可靠。
- **消融实验**：
  - Generator-only vs Full SAGE（表1）：隔离动作条件生成的效果。
  - SAGE prior top vs Full SAGE（表2）：隔离LeWM引导优化的效果。
  - 不同时长调度研究（表3）：在PushT上H=50,75,100,125,150下测试多种时长组合，共10个条件。
  - 模型容量研究（表8）：小参数量 vs 标准参数量在PushT上的比较。
- **充分性评价**：实验设计较为充分，覆盖了核心组件的独立贡献、不同规划步长、不同调度策略和容量影响。对比方法（PRISM）有基线比较。但缺少与其他近期工作（如HWM、VLWM、FF-JEPA）的直接定量对比，可能因为这些方法并非完全“冻结世界模型”设定，但可以作为未来参考。

## 6. 主要结论与发现

- **核心结论**：子目标条件动作生成能显著提升长时程规划性能，且不影响短时程性能。
- **量化结果**：
  - 当H=150时，PushT成功率从Base CEM的12.7%提升至64.7%；Cube从26.7%提升至67.3%。
  - 即使在H=25短时程，Full SAGE也优于Base CEM（PushT 94.0% vs 90.0%）。
- **消融揭示**：
  - Generator-only在长时程仍有大幅提升（H=150时PushT 58.7%），说明局部子目标本身很有价值。
  - Full SAGE进一步超越了Generator-only，说明子目标条件动作生成有明显增量收益。
  - SAGE prior top效果远差于Full SAGE（H=150时16.0% vs 64.7%），证明LeWM引导的CEM优化不可或缺。
- **时长调度影响**：更频繁的重规划通常更好，但不同时长顺序也会影响结果，提示未来可学习自适应时长策略。

## 7. 优点

- **创新性**：首次将子目标预测与动作生成条件化结合，形成一种“先验条件规划器”，思路清晰，与冻结世界模型兼容。
- **简洁有效**：不改变世界模型架构或训练过程，仅通过在规划阶段引入学习到的子目标与动作先验即可实现显著提升，实用性强。
- **多尺度支持**：同一生成器支持多种时长（15/20/25），可灵活组合调度，适应不同任务需求。
- **消融设计严谨**：逐步剥离核心组件，清晰展示了每个部分的贡献，验证充分。

## 8. 不足与局限

- **实验对比范围有限**：仅与基础CEM和PRISM对比，未与其他近期分层世界模型方法（如HWM、VLWM）或更好策略（如FF-JEPA）进行直接定量比较。
- **固定调度策略**：当前调度是手动预设的，未学习自适应规划深度或时长，论文虽指出该方向但未实现。
- **仅两个任务**：PushT和Cube虽具代表性，但均为仿真环境，缺乏真实机器人实验和对更复杂任务（如多阶段、稀疏奖励）的验证。
- **未报告训练计算量**：缺乏GPU型号、训练时间等具体信息，不利于复现和效率评估。
- **潜在偏差风险**：训练数据来源于专家演示，子目标生成器可能存在对特定行为模式的过拟合，在分布外场景下的泛化性未知。
- **CEM计算成本**：虽然提案数固定，但子目标生成和动作生成也需额外前向计算，相比纯CEM在时间开销上略有增加（但论文未量化）。

（完）
