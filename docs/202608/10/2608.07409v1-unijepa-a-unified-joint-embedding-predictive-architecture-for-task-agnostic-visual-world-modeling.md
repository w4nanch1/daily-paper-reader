---
title: "UniJEPA: A Unified Joint-Embedding Predictive Architecture for Task-Agnostic Visual World Modeling"
title_zh: UniJEPA：一种用于任务无关视觉世界建模的统一联合嵌入预测架构
authors: "An Lanji, Dawei Liu, Jin Li, Haoran Xu, Mei Chen, Yu Tian"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.07409v1"
tags: ["query:world"]
score: 8.0
evidence: 统一JEPA架构，面向任务无关的视觉世界建模
tldr: 现有JEPA在图像与视频上各自为政，图像级光度预测与视频级时间预测被分离为不同配方。UniJEPA在共享潜在空间联合学习两类预测，仅用下一嵌入损失和高斯正则器即可防坍缩，无需EMA或预训练编码器。同一空间支持可控抽象，经行动条件后训练后可将目标特征作为预测目标实现零样本规划。在图像、视频和控制基准上匹配或超越任务特定JEPA，仅需一个损失超参数，规划速度比生成模型快数十倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.07409v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 727, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1685, \"height\": 948, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 802, \"height\": 412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 830, \"height\": 497, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 826, \"height\": 497, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 833, \"height\": 497, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.07409v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1696, \"height\": 934, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.07409v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 866, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1479, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1072, \"height\": 461, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1006, \"height\": 352, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1322, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.07409v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1776, \"height\": 396, \"label\": \"Table\"}]"
motivation: 现有JEPA将光度预测与时间预测分离为不同配方，各自独立编码器和正则器，难以统一图像与视频世界建模。
method: UniJEPA在共享潜在空间联合学习两类预测，用下一嵌入损失和高斯正则器防坍缩，无需EMA或预训练编码器。
result: 在图像、视频和控制基准上匹配或超越任务特定JEPA，仅需一个损失超参数，规划速度比生成模型快数十倍。
conclusion: 统一JEPA同时建模不变结构与等变动态，支持可控抽象与零样本规划，为视觉世界建模提供通用范式。
---

## 摘要
联合嵌入预测架构（JEPAs）已成为在紧凑潜在空间中对世界模型进行自监督学习的一种原则性框架，然而现有方法较为碎片化：一些方法在潜在空间中预测单幅图像的掩蔽部分（I-JEPA），另一些方法学习预测全局光度变换（图像世界模型），而视频级JEPAs预测未来时间状态，并针对动作条件规划进行后训练（V-JEPA~2、DINO-World、DINO-WM）。这些目标被视为具有独立编码器、预测器和防坍塌正则化器的不同配方，阻碍了单一模型统一图像级和视频级的世界建模。我们提出UniJEPA，一种统一的JEPA，在共享潜在空间中联合学习光度预测（图像级变换）和时间预测（视频级下一状态动态）。一个由下一嵌入预测损失和高斯正则化器组成的单一端到端目标，产生了一个可证明防坍塌的编码器-预测器对，可直接从原始像素训练，无需EMA、停止梯度或预训练编码器。我们表明，同一潜在空间支持可控抽象：光度预测学习不变结构，而时间预测学习等变动态。在离线轨迹上进行动作条件后训练后，UniJEPA通过将目标特征视为预测目标来实现零样本规划。在图像、视频和控制基准测试中，UniJEPA在仅需单个损失超参数的情况下，达到或超过了任务特定的JEPA，并且在相当精度下，规划速度比生成式世界模型快数十倍。

## Abstract
Joint-Embedding Predictive Architectures (JEPAs) have emerged as a principled framework for self-supervised learning of world models in compact latent spaces, yet existing methods are fragmented: some predict masked parts of a single image in latent space (I-JEPA), others learn to predict global photometric transformations (Image World Models), while video-scale JEPAs predict future temporal states and are post-trained for action-conditioned planning (V-JEPA~2, DINO-World, DINO-WM). These objectives are treated as distinct recipes with separate encoders, predictors, and anti-collapse regularizers, hindering a single model from unifying image-level and video-level world modeling. We present UniJEPA, a unified JEPA that jointly learns photometric prediction (image-level transformations) and temporal prediction (video-level next-state dynamics) in one shared latent space. A single end-to-end objective, composed of a next-embedding prediction loss and a Gaussian regularizer, yields a provably anti-collapse encoder-predictor pair trainable from raw pixels without EMA, stop-gradient, or pre-trained encoders. We show that the same latent space supports controllable abstraction: photometric prediction learns invariant structure while temporal prediction learns equivariant dynamics. After action-conditioned post-training on offline trajectories, UniJEPA enables zero-shot planning by treating goal features as prediction targets. On image, video, and control benchmarks, UniJEPA matches or surpasses task-specific JEPAs while requiring a single loss hyperparameter, and plans up to tens of times faster than generative world models at comparable accuracy.

---

## 论文详细总结（自动生成）

## UniJEPA 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：联合嵌入预测架构（JEPA）是 LeCun 提出的自监督学习范式，其核心思想是在潜在空间中预测未来或掩蔽内容的表示，而非在像素空间中重建。与生成式世界模型相比，JEPA 由于丢弃了可预测的、与任务无关的细节，在数据和计算效率上更具优势。
- **核心问题**：现有 JEPA 方法高度碎片化，不同变体在各自独立的潜在空间中解决不同任务：
  - 图像级 JEPA（如 I-JEPA、IWM）处理掩蔽预测或光度变换预测；
  - 视频级 JEPA（如 V-JEPA 2、DINO-World）处理时间状态预测；
  - 规划类 JEPA（如 DINO-WM）在预训练编码器之上单独训练动作条件预测器。
  - 这些方法各自配备独立的编码器、预测器和防坍缩正则器，导致图像级与视频级世界模型无法组合使用，潜在空间不兼容。
- **论文主张**：图像级与视频级预测是对同一世界模型的两种视角，应当在一个统一的共享潜在空间中同时建模。这一统一具有实际意义——一个既能解释全局变换下场景如何变化、又能预测场景随时间如何演化的世界模型，更具迁移性、样本效率，并能在从表征学习到具身规划的更广泛下游任务中发挥作用。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

**核心思想**：UniJEPA 将光度预测（图像级变换）和时间预测（视频级下一状态动态）统一为同一潜在预测任务的两种实例，仅用一个共享编码器和一个共享预测器，在单一端到端目标下优化。

- **统一目标**：联合优化两个损失项：
  - 光度预测损失：$L_{photo} = E_{x,\tau}\left[\|g_\psi(f_\theta(x), \tau) - f_\theta(\tau(x))\|_2^2\right]$
  - 时间预测损失：$L_{temp} = E_{(x_t,a_t,x_{t+1})}\left[\|g_\psi(f_\theta(x_t), a_t) - f_\theta(x_{t+1})\|_2^2\right]$
  - 总损失：$L_{UniJEPA} = L_{photo} + L_{temp} + \alpha R$，仅需单个标量超参数 α。
- **可证明防坍缩的正则化**：引入高斯正则化器 $R(\theta) = E_{x,u}[\chi_1^2((u^T z)^2)]$，其中 u 为随机单位向量，χ²₁ 为偏离标准正态的平方马氏距离。论文通过定理 3.1 证明：当正则化器最小化到 R ≤ ε 时，编码器不可能为常数，且潜在分布的协方差矩阵最小特征值 λ_min ≥ 1 − O(ε)，即潜在分布非退化。该正则化器不需要 EMA、停止梯度或预训练编码器。
- **可控抽象（Invariance–Equivariance 谱）**：光度预测鼓励编码器对光度扰动不变（invariance），时间预测鼓励表示对时间动态等变（equivariance）。通过调节两类损失的权重，可以控制表征的抽象层级（命题 3.2）。
- **动作条件后训练与零样本规划**：预训练完成后冻结编码器，在离线轨迹数据上对预测器进行动作条件后训练。规划时将目标特征作为预测目标，通过 MPC（模型预测控制）与基于采样的优化求解动作序列。由于预测在潜在空间中进行，每次 rollout 仅需一次预测器前向传播。

**训练与规划流程（Algorithm 1）**：
1. 预训练：从原始像素采样（x, τ）和（x_t, a_t, x_{t+1}），按式（4）更新 θ 和 ψ；
2. 后训练：冻结 θ，在离线轨迹 D 上按式（2）更新 ψ；
3. 规划：给定当前观测 z₀ 和目标 z_g，求解式（5）中的 MPC 优化，返回动作序列。

### 3. 实验设计：数据集 / 场景、Benchmark、对比方法

**数据集与场景**：
- 图像表征：ImageNet-1k（线性探针与微调）；
- 视频理解：Something-Something-v2（运动理解）、Epic-Kitchens-100（动作预期）；
- 诊断性视频评测：Perception Test、TempCompass（时间推理）；
- 规划/控制：离线控制套件，覆盖迷宫导航、推操作、多粒子控制等目标到达任务。

**基准对比方法**：
- 任务特定 JEPA：I-JEPA、IWM、V-JEPA、V-JEPA 2、DINO-WM、LeWorldModel；
- 对比学习：SimCLR、DINOv2；
- 生成式世界模型：Sora（Brooks et al.）、Stable Video Diffusion（Blattmann et al.）。

**主要评估指标**：ImageNet top-1 准确率、SSv2 top-1、EK-100 recall@5、规划成功率、规划速度倍率、损失超参数数量。

### 4. 资源与算力

- 论文 A 节提到：ViT-Small/16 编码器（15M 参数）用于控制任务，ViT-Large 用于大规模图像/视频表征学习；预测器为轻量 MLP/ViT；使用 Adam 优化器，从原始像素端到端训练。
- 3.6 节提到：15M 参数变体在单张 GPU 上仅需数小时即可完成训练（与 LeWorldModel 一致）。
- **明确说明**：论文未提供具体的 GPU 型号、GPU 数量或精确的训练时长（以小时/天为单位）等详细算力信息，仅给出"单 GPU 数小时"这一粗略描述。

### 5. 实验数量与充分性

- **实验组数**：论文包含 4 张主要表格和 7 张图：
  - Table 1：主结果对比（表征质量、视频理解、规划成功率与速度、损失超参数数量）；
  - Table 2：消融实验 I（损失组件、正则化权重、模型规模、有无正则器的坍缩对比）；
  - Table 3：消融实验 II（MPC 时域长度、候选 rollout 数量、后训练数据量、是否冻结编码器）；
  - Table 4：细粒度评估（两个编码器规模下的 ImageNet 线性/微调精度、诊断视频基准、不同时域的动作预期）。
  - 另含规划按任务族的成功率分解、收敛曲线、效率-精度热力图、正则化权重敏感性等实验。
- **充分性评估**：
  - 优点：消融设计较全面，覆盖了各类损失的必要性、正则化器权重的影响、编码器规模、规划超参数等关键因素；与任务特定 JEPA、对比学习、生成式世界模型等多类方法进行了对比；在图像、视频、控制三大场景上均有评估。
  - 不足：规划实验未使用标准统一的具身控制基准（如 DMLab、Atari100k 或 RoboMimic 的完整协议），而是"离线控制套件"；对比的生成式世界模型（Sora、SVD）为外部引用，未在同一实验条件下进行公平的计算成本比较；缺少与 V-JEPA 2-AC 及 DINO-WM 在相同规划任务/数据集上的并排详细比较（尽管整体指标有对比）。

### 6. 论文的主要结论与发现

- **统一目标有效**：仅用一个共享编码器+共享预测器，就能同时完成光度预测和时间预测，且单一平方误差目标在紧凑潜在空间中训练稳定。
- **防坍缩保证成立**：高斯正则化器无需 EMA/stop-gradient/预训练编码器即可防止坍缩；当 α=0 时表征退化到 rank 3（验证了定理 3.1）。
- **性能超越任务特定方法**：
  - ImageNet 线性探针 74.9%（优于 IWM 的 73.5%，接近 DINOv2 的 81.3%）；
  - SSv2 上 78.1%（超过 V-JEPA-2 的 77.3%）；
  - EK-100 动作预期 recall@5 40.6%（超过 V-JEPA-2 的 39.7%）；
  - 规划成功率 75.8%（超过 DINO-WM 的 74.6% 与 LeWorldModel 的 68.9%），规划速度 44×（超过 DINO-WM 的 12×，接近 LeWorldModel 的 48×）。
- **可控抽象**：光度损失促进不变性、时间损失促进等变性，两者可通过权重调节实现表征抽象层级的控制。
- **规划效率与精度兼得**：潜在空间 MPC 规划比像素级生成式世界模型快数十倍且精度更高，在 H=5、C=64 时达到最优平衡。
- **数据效率**：仅用 10% 的轨迹数据后训练即可达到 72.0% 的规划成功率，表明模型具有样本效率优势。

### 7. 优点

- **统一性贡献**：首次将图像级与视频级 JEPA 统一到单一共享潜在空间，消除了此前各类 JEPA 潜空间不兼容的碎片化问题，是一个新颖且有价值的系统贡献。
- **简洁性**：仅需一个损失超参数，无 EMA、无 stop-gradient、无预训练编码器，大幅降低训练复杂度。
- **理论保证**：给出防坍缩的正式定理证明（尽管证明本身较简洁），为方法提供了理论支撑，而非纯工程试错。
- **可控抽象视角**：通过两类损失的加权插值，提供了一个调节表征不变/等变谱系的实用旋钮，具有解释价值。
- **效率显著**：规划速度比生成式世界模型快两个数量级以内（44×），同时保持或超过任务特定方法的精度。
- **实验覆盖较广**：同时评估图像、视频、控制三大场景，且包含较丰富的消融分析。

### 8. 不足与局限

- **理论证明较简略**：定理 3.1 的证明依赖"常数编码器使 R 无界"这一直观论证，缺少对 ε 与模型容量、维度 d 之间依赖关系的定量刻画；"R≤ε 推出 λ_min≥1−O(ε)"这一步过于简短，严谨性有待加强。
- **架构限制**：光度与时间预测共享同一预测器，可能不足以处理需要不同抽象层次的长时程或多模态任务。
- **正则化器容量疑虑**：高斯正则化器虽然可证明防坍缩，但在极大规模模型下可能限制表征容量。
- **多模态与指令缺失**：不支持语言指令输入，与视觉-语言-动作（VLA）通用智能体相比交互能力有限。
- **算力披露不足**：未明确报告 GPU 型号、数量及精确训练时间，可复现性信息不完整。
- **规划基准弱于通用标准**：规划实验采用自定义离线控制套件，未使用公认的标准控制/机器人基准；与 DINO-WM 等的规划成功率对比可能受数据集难度差异影响，公平性有待进一步审视。
- **图表缺失**：图 7（不变-等变谱系）和定性图（Figure ??）在正文中存在引用缺失或未展示完整内容，影响读者对定性结果和可控抽象部分的理解。
- **部署风险**：作者在影响声明中承认世界模型的预测动态可能不完美，在安全关键或自主场景中部署前需仔细验证。

---

（完）
